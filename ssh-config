# NOTE - this file goes in `~/.ssh/config` (it doesn't have a file extension)
# NOTE - uncomment code to apply config

# NetShare (cell phone tethering) proxy config (from: https://adangel.org/2020/10/15/github-behind-proxy/)
# NOTE - requires `ncat` (`sudo apt install ncat`)
# NOTE - NetShare apt config at `/etc/apt/apt.conf.d/proxy.conf`
# Host github.com
# 	Hostname ssh.github.com
#   Port 443
#   ProxyCommand ncat --proxy-type http --proxy 192.168.49.1:8282 %h %p
